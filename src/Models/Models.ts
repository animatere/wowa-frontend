export interface Card {
    id: string;
    type: string;
    cardText: string;
    category: string;
  }

  export interface repos {
    id: string;
    name: string;
    html_url: string;
    description: string;
}